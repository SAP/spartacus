import { ServerOptions } from './model';
/**
 * Returns a factory function which resolves the server request URL.
 */
export declare function serverRequestUrlFactory(options?: ServerOptions): Function;
