import { ServerOptions } from './model';
/**
 * Returns a factory function which resolves the server request origin.
 */
export declare function serverRequestOriginFactory(options?: ServerOptions): Function;
