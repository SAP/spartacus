import { Address } from '@spartacus/core';
/**
 * Get strings for phone and mobile numbers
 */
export declare function getAddressNumbers(address: Address, textPhone: string, textMobile: string): string | undefined;
