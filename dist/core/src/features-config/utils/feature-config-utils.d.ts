import { Config } from '../../config/config-tokens';
export declare function isFeatureLevel(config: Config, level: string): boolean;
export declare function isFeatureEnabled(config: Config, feature: string): boolean;
