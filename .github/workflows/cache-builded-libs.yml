on:
  push:
    branches:
      - develop
      - 'epic/**'
      - 'release/**'
      - cxspa-2141-test-develop-simulate
name: Cache libs (dist)
jobs:
  cacheBuildedLibs:
    name: Builded libs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Cache builded libs
        uses: ./.github/cache-builded-libs
