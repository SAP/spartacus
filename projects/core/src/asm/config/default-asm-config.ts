import { AsmConfig } from './asm-config';

export const defaultAsmConfig: AsmConfig = {
  asm: {
    sessionTimer: {
      startingDelayInSeconds: 600,
    },
    customeSearch: {
      maxResults: 20,
    },
  },
};
