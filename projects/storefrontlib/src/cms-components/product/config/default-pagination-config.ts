import { PaginationConfig } from './pagination-config';

export const defaultPaginationConfig: PaginationConfig = {
  pagination: {
    infiniteScroll: {
      active: false,
      button: false,
      limit: 0,
    },
  },
};
