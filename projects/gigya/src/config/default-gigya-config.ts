import { GigyaConfig } from './gigya-config';

export const DEFAULT_GIGYA_CONFIG: GigyaConfig = {
  gigya: [
    {
      baseSite: 'electronics-cdc',
      javascriptUrl: '',
      sessionExpiration: 3600,
    },
  ],
};
