/**
 * Codes to identify type of errors
 *
 * Note: These are just examples, not the actual types.
 */

export const enum CxError {
  UnknownHttpResponseError = 'HR0001',
  UnknownServerRenderError = 'SR0001',
}
