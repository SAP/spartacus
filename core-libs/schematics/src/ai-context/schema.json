{
  "$schema": "http://json-schema.org/schema",
  "$id": "SpartacusAiContextSchematics",
  "title": "Spartacus AI Context",
  "type": "object",
  "properties": {
    "aiTools": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": ["claude", "agents"]
      },
      "default": ["claude"],
      "description": "Where to put Spartacus Development AI skills? (leave empty for none)",
      "x-prompt": {
        "type": "list",
        "multiselect": true,
        "message": "Where to put Spartacus Development AI skills? (leave empty for none)",
        "items": [
          {
            "value": "claude",
            "label": ".claude (.claude/skills/spartacus-developer/)"
          },
          {
            "value": "agents",
            "label": ".agents (.agents/skills/spartacus-developer/)"
          }
        ]
      }
    },
    "debug": {
      "description": "Display additional details during the running process.",
      "type": "boolean",
      "default": false
    }
  },
  "required": []
}
