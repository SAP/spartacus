Add a cookie consent banner to this Spartacus storefront. It should:
- Appear at the bottom of the page on a user's first visit
- Show an "Accept" and a "Decline" button
- Persist the user's choice so the banner does not reappear on subsequent visits
- Not break server-side rendering (the app runs with SSR)
- Be manageable through the CMS so it can be enabled or disabled from the backend
