{
  "name": "bff-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "implicitDependencies": ["bff"],
  "targets": {
    "e2e": {
      "executor": "@nx/vitest:test",
      "options": {
        "config": "{projectRoot}/vitest.config.mts"
      }
    },
    "test": {
      "executor": "nx:noop",
      "options": {
        "command": "echo 'Unit tests are disabled.  This project hosts e2e tests triggered by the 'e2e' target."
      }
    }
  }
}
