{
  "name": "subscription-billing",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "feature-libs/subscription-billing",
  "prefix": "cx",
  "targets": {
    "build": {
      "executor": "./tools/build-lib:declaration-merging",
      "options": {
        "tsConfig": "feature-libs/subscription-billing/tsconfig.lib.json",
        "project": "feature-libs/subscription-billing/ng-package.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "feature-libs/subscription-billing/tsconfig.lib.prod.json"
        }
      }
    },
    "test": {
      "executor": "@angular-devkit/build-angular:karma",
      "options": {
        "main": "feature-libs/subscription-billing/test.ts",
        "tsConfig": "feature-libs/subscription-billing/tsconfig.spec.json",
        "polyfills": ["zone.js", "zone.js/testing"],
        "karmaConfig": "feature-libs/subscription-billing/karma.conf.js"
      }
    },
    "test-jest": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run test:schematics",
        "cwd": "feature-libs/subscription-billing"
      }
    },
    "lint": {
      "executor": "@angular-eslint/builder:lint",
      "options": {
        "lintFilePatterns": [
          "feature-libs/subscription-billing/**/*.ts",
          "feature-libs/subscription-billing/**/*.html"
        ]
      }
    },
    "build-schematics": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/schematics/**/*.js",
        "{projectRoot}/schematics/**/*.js.map",
        "{projectRoot}/schematics/**/*.d.ts"
      ],
      "options": {
        "cwd": "feature-libs/subscription-billing",
        "commands": [
          "npm run clean:schematics",
          "../../node_modules/.bin/tsc -p ./tsconfig.schematics.json"
        ],
        "parallel": false
      },
      "cache": true
    }
  },
  "tags": ["type:feature"]
}
