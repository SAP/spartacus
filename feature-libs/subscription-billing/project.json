{
  "name": "subscription-billing",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "feature-libs/subscription-billing",
  "prefix": "cx",
  "targets": {
    "build": {
      "executor": "./tools/build-lib:declaration-merging",
      "options": {
        "tsConfig": "feature-libs/subscription-billing/tsconfig.lib.json",
        "project": "feature-libs/subscription-billing/ng-package.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "feature-libs/subscription-billing/tsconfig.lib.prod.json"
        }
      }
    },
    "test-vitest": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx vitest run --config vitest.config.ts",
        "cwd": "{projectRoot}"
      }
    },
    "test-jest": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run test:schematics",
        "cwd": "feature-libs/subscription-billing"
      }
    },
    "lint": {
      "executor": "@angular-eslint/builder:lint",
      "options": {
        "lintFilePatterns": [
          "feature-libs/subscription-billing/**/*.ts",
          "feature-libs/subscription-billing/**/*.html"
        ]
      }
    }
  },
  "tags": ["type:feature"]
}
