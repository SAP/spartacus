/**
 * Identifiers of outlets for the pickup in store feature.
 */
export enum PickupInStoreOutlets {
  /** The outlet for selecting either delivery or pickup in store. */
  PICKUP_OPTION = 'cx-pickup-option',
}
