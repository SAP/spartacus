import { Injectable, OnInit } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class LoginComponentService {
  constructor(
  ) { }

  myAccountForCdp(): boolean{
      return true;
  }
  }

