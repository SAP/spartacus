import { LayoutConfig } from '@spartacus/storefront';

export const defaultAccountSummaryLayoutConfig = {
  layoutSlots: {
    CompanyPageTemplate: {
      slots: ['BodyContent'],
    },
  },
} as LayoutConfig;
