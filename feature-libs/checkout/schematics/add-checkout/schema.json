{
  "$schema": "http://json-schema.org/schema",
  "$id": "CheckoutSchematics",
  "title": "Checkout Schematics",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    },
    "lazy": {
      "type": "boolean",
      "description": "Lazy load the Checkout feature.",
      "default": true
    },
    "features": {
      "type": "array",
      "uniqueItems": true,
      "items": {
        "enum": [
          "Checkout",
          "Checkout-B2B",
          "Checkout-Scheduled-Replenishment"
        ],
        "type": "string"
      },
      "default": ["Checkout"],
      "x-prompt": {
        "message": "Which features would you like to set up from the Checkout library?",
        "type": "list",
        "items": [
          {
            "value": "Checkout",
            "label": "Checkout"
          },
          {
            "value": "Checkout-B2B",
            "label": "Checkout (b2b features)"
          },
          {
            "value": "Checkout-Scheduled-Replenishment",
            "label": "Checkout Scheduled Replenishment (b2b feature)"
          }
        ]
      }
    }
  },
  "required": []
}
