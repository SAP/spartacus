import { LayoutConfig } from '@spartacus/storefront';

export const organizationLayoutConfig = {
  layoutSlots: {
    CompanyPageTemplate: {
      slots: ['BodyContent'],
    },
  },
} as LayoutConfig;
