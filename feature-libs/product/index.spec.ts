// this test can be removed when any code and tests are added to this lib
// otherwise pipeline fails when run `ng test product --code-coverage`
it('dummy test', () => {
  expect(true).toBe(true);
});
