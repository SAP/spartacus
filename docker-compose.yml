version: '3.4'

services:
  storefront-poc-ssr-front:
    build:
      context: .
      target: ssr-front
    ports:
      - "80:80"
    restart: unless-stopped
  storefront-poc-ssr-back:
    build:
      context: .
      target: ssr-back
    ports:
      - "4200:4200"
    restart: unless-stopped
